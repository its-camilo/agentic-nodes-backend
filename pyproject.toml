[project]
name = "agentic-nodes-backend"
version = "0.1.0"
description = "Supply Chain AI Agents API"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "pydantic>=2.0.0",
    "httpx>=0.27.0",
    "uvicorn[standard]>=0.30.0",
]

[project.scripts]
app = "main:app"
