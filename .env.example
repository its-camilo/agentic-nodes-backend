PORT=8000
DEBUG=True

# LLM Provider: "openai" or "openrouter" (one is required)
LLM_PROVIDER=openai

# OpenAI
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini

# OpenRouter (alternative to OpenAI)
OPENROUTER_API_KEY=sk-or-your-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=openai/gpt-4o-mini

# CORS: comma-separated origins (required when using credentials).
# Defaults include https://its-camilo.github.io and localhost. Override if your frontend URL differs.
# ALLOWED_ORIGINS=https://its-camilo.github.io,http://localhost:5173
# ALLOWED_ORIGINS=

# World data loaded from world_data.json; routes + negotiation + summary use AI.
